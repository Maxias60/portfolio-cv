<div class="external-project-card-grid">
  <slot />
</div>

<style>
  .external-project-card-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1rem;
    width: 100%;
    margin: 2rem 0;
    align-items: stretch;
  }

  @media (max-width: 700px) {
    .external-project-card-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
